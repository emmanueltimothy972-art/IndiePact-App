VITE_DEV_AUTH_BYPASS=false

# Local API server port — used by the Vite dev proxy to forward /api/* requests.
# Must match the PORT that the api-server workflow is assigned in this Replit.
# Check the api-server workflow logs for "Server running on port XXXX" to confirm.
API_SERVER_PORT=8082
